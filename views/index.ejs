<%- include('partials/head') %>

<main>
    <div>
        <h1>Blog</h1>
        <h2>We love web</h2>
    </div>

    <div class="search-bar">
        <form class="search-box" method="get" action="/">
            <input type="text" id="search-input" autocomplete="off" name="searchbar">
            <button aria-label="Search">
                <img src="assets/img/search-icon.svg" alt="">
            </button>
        </form>
        <div class="result-box">
        </div>
    </div>

    <ul class="blog-post-list">
        <% posts.forEach(post => { %>
            <li>
                <article>
                    <a href="https://www.voorhoede.nl/en/blog/<%- post.slug %>">
                        <h3><%- post.title %></h3>
                    </a>
                    <time date="<%- post.updatedAt %>"><%- post.updatedAt %></time>
                    <div class="authors">
                        <% post.authors.forEach(author => { %>
                                <img src=<%- author.image.url %> alt="" loading="lazy" class="author-img" width="65">
                            <% }) %>
                        <p> By
                            <% post.authors.forEach(author => { %>
                                <%= author.name %>
                            <% }) %>
                        </p>
                    </div>
                </article>
            </li>
        <% }) %>
    </ul>

</main>

<%- include('partials/foot') %>